//module.exports = client => {
   // if(!userData[sender.id]) userData[sender.id] = {
   //     messagesSent: 0
    //}
   // userData[sender.id].messages.Sent++;
   // fs.writeFile('userData.json', JSON.stringify(userData), (err) => {
   //     if (err) console.error(err);
    //})
//};